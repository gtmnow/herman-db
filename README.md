# herman-db

Canonical schema project for the shared Herman Postgres database.

This repo owns:

- the single migration stream for the shared Herman DB
- schema governance and ownership metadata
- shared contract documentation
- baseline and cleanup migration history

This repo does not own application business logic.
